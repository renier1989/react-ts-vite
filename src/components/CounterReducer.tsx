const initialState = {
    contador:0,
}

const contadorReducer = (state,action)=>{
    switch (action) {
        case 'incrementar':
            
            break;
    
        default:
            break;
    }
}


export const CounterReducer = () => {
    return (
        <>
            <div>Counter Reducer : <span>{10}</span> </div>
        </>
    )
}
